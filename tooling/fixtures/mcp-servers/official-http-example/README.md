# Official HTTP Example

This inert package exercises one fixed HTTPS Streamable HTTP MCP endpoint.
Installing or listing it must not connect to the endpoint. Connection and
standard OAuth or anonymous confirmation happen only during Convax setup.
