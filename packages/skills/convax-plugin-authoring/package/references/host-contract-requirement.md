# Host contract requirement: <generic name>

## User problem

<Observable need without prescribing a Host implementation or naming one Plugin
as the Host behavior switch.>

## Missing generic contract

- Current Catalog version:
- Closest published contracts:
- Exact missing capability or contribution:
- Intended audiences:
- Scope and side effect:
- Required grant:
- Bounded request and response:
- Stable errors:
- Cancellation and stale-scope behavior:

## Alternatives

<Existing APIs, Plugin-to-Plugin capabilities, or workflow changes considered.>

## Security and compatibility

<Principal, least authority, resource exposure, credentials, executable/renderer
boundaries, Catalog SemVer effect, and old-Host behavior.>

## Falsifiable acceptance tests

1. <Evidence that proves the contract is generic and sufficient.>
2. <Failure, cancellation, and stale-scope evidence.>
3. <Evidence that would show the proposal should be rejected.>

## Automated publication transition

- Exact affected package versions:
- Technical blocker while the contract is absent:
- Expected Catalog API ids:
- Once published, exact Catalog contract digests:
- Plugin-side changes after the generated Catalog satisfies the requirement:

Do not add a reviewer, approval status, receipt, Environment, or manual bypass.
The exact package becomes ready only when current Catalog/package validation passes
and the technical blocker is removed.
