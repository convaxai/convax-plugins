{
  "name": "@microvoid/convax-shortdrama-router-mcp",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "bin": {
    "convax-shortdrama-router-mcp": "dist/convax-shortdrama-router-mcp"
  },
  "scripts": {
    "build": "bun build --minify --target=bun --outfile dist/convax-shortdrama-router-mcp src/index.ts",
    "build:release": "bun run build:release:darwin-arm64",
    "build:release:darwin-arm64": "bun build --minify --target=bun --outfile dist/darwin-arm64/convax-shortdrama-router-mcp src/index.ts",
    "clean": "rm -rf dist",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "shortdrama-router": "0.5.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "5.9.3"
  }
}
